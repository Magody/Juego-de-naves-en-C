#ifndef CODIGO_VOTADO_H_INCLUDED
#define CODIGO_VOTADO_H_INCLUDED


/*int x=35,y=10;
    int caracterPrin=15;

    dibujarCaracter(x,y,caracterPrin);

    if(kbhit())
        {
            char tecla = getch(); //obtiene tecla

            borrarCaracter(x,y,' ');
            eventosTeclado(tecla,&x,&y,&enJuego);
            dibujarCaracter(x,y,caracterPrin);

            Sleep(30);
        }

        void dibujarCaracter(int x,int y,char c)
{gotoxy(x,y); putchar(c);}

void borrarCaracter(int x,int y,char c)
{gotoxy(x,y); putchar(c);}*/


#endif // CODIGO_VOTADO_H_INCLUDED
